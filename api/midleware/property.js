/* eslint-disable linebreak-style */
/* eslint-disable no-undef */
// export function queryType(req, res, next) {
//   const adType = loans.filter(ad => ad.type === req.query.type);
//   if (typeof req.query.type !== 'undefined') return res.send(adType);
//   next();
// }
